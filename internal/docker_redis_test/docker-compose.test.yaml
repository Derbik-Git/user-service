version: "3.9"

services:
  redis:
    image: redis:7.2-alpine
    container_name: test-redis
    ports:
      - "6380:6380"
    command: >
      redis-server
      --appendonly no
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 2s
      timeout: 2s
      retries: 10