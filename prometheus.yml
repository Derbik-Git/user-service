global:
  scrape_interval: 5s 

scrape_configs:
  - job_name: 'user-service'
    static_configs:
      # Прометеус будет стучаться в контейнер user-service по порту 2112
      - targets: ['user-service:2112']